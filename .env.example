# ================================
# SNAPGO WEBSITE ENVIRONMENT CONFIG
# ================================

# ================================
# DATABASE (REQUIRED)
# ================================

# Option 1: Vercel Postgres (Recommended for Vercel)
# POSTGRES_URL="postgres://..."
# POSTGRES_PRISMA_URL="postgres://..."
# POSTGRES_URL_NON_POOLING="postgres://..."

# Option 2: PlanetScale (MySQL-compatible)
# DATABASE_URL="mysql://username:password@host/database?sslaccept=strict"

# Option 3: Supabase
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres"

# Option 4: Railway
# DATABASE_URL="mysql://root:password@containers-us-west-xxx.railway.app:7890/railway"

# Local Development (MySQL)
DATABASE_URL="mysql://your_username:your_password@localhost:3306/snapgo_db"

# ================================
# SITE CONFIGURATION
# ================================

# Site URL (used for sitemap, metadata, canonical URLs)
NEXT_PUBLIC_SITE_URL="https://snapgo.in"

# ================================
# FILE STORAGE (REQUIRED FOR VERCEL)
# ================================

# Option 1: Vercel Blob (Recommended - auto-configured when connected)
# BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# Option 2: Cloudinary
# CLOUDINARY_CLOUD_NAME="your_cloud_name"
# CLOUDINARY_API_KEY="your_api_key"
# CLOUDINARY_API_SECRET="your_api_secret"

# Option 3: AWS S3
# AWS_ACCESS_KEY_ID="your_access_key"
# AWS_SECRET_ACCESS_KEY="your_secret_key"
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="your-bucket-name"

# ================================
# GOOGLE ANALYTICS (OPTIONAL)
# ================================

# GA4 Measurement ID for tracking
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# For Admin Dashboard Analytics (requires GA4 API setup)
# GA_PROPERTY_ID="123456789"
# GOOGLE_APPLICATION_CREDENTIALS='{"type":"service_account",...}'

# ================================
# ADMIN CONFIGURATION
# ================================

# Override default admin credentials (optional)
# Default: username=admin, password=snapgo2024
# ADMIN_USERNAME="admin"
# ADMIN_PASSWORD_HASH="your_sha256_hash"

# ================================
# VERCEL DEPLOYMENT CHECKLIST
# ================================
#
# 1. DATABASE SETUP:
#    - Go to Vercel Dashboard > Your Project > Storage
#    - Click "Create Database" > Choose Postgres or connect PlanetScale
#    - Environment variables will be auto-added
#
# 2. FILE STORAGE SETUP:
#    - Go to Vercel Dashboard > Your Project > Storage
#    - Click "Create Store" > Blob
#    - BLOB_READ_WRITE_TOKEN will be auto-added
#
# 3. AFTER CONNECTING DATABASE:
#    - Update prisma/schema.prisma:
#      Change: provider = "mysql"
#      To: provider = "postgresql" (if using Vercel Postgres)
#    - Run: npx prisma generate
#    - Run: npx prisma db push (or prisma migrate deploy)
#
# 4. SEED DATA:
#    - Run: npx prisma db seed (if seed script exists)
#
# 5. ENVIRONMENT VARIABLES:
#    - Add all required variables in Vercel Dashboard
#    - Settings > Environment Variables
#
